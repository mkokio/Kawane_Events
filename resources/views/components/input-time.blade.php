<div class="mb-3">
    <label for="{{ $for }}" class="form-label">{{ __($label) }}</label>
    <br/>
    <input type="time" id="{{ $for }}" name="{{ $for }}" class="form-control" {{ $attributes }}>
</div>