<div class="mb-3">
    <label for="{{ $for }}" class="form-label">{{ __($label) }}</label>
    <br/>
    <input type="time" id="{{ $for }}" name="{{ $for }}" class="form-control border-gray-300 focus:border-indigo-500 focus:ring-indigo-500 rounded-md shadow-sm" {{ $attributes }}>
</div>